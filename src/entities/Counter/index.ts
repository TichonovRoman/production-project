import {counterReducers} from "./modal/slice/counterSlice";
import {Counter} from "./ui/Counter";
import type {CounterSchema} from "./modal/types/counterSchema";

export {
    counterReducers,
    Counter,
    CounterSchema
}